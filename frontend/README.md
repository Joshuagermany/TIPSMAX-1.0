# TIPSMAX 1.0 Frontend

React + TypeScript 기반 웹 애플리케이션

## 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build
```

## 환경 변수

`.env` 파일 생성 (선택사항):
```
VITE_API_BASE_URL=http://localhost:8000
```
